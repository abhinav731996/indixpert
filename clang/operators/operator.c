#include <stdio.h>

int main()
{
    // int firstnum = 10;
    // int secnum = 20;
    // // firstnum = firstnum*5;
    // printf("%d %d %d",secnum, firstnum);
    int num = 100;
    num--;
    --num;
    num += 80;
    num--;
    printf("%d", num);
    // printf("%d", num++);
    return 0; 
}